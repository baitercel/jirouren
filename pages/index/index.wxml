<view class="container flex-wrap flex-direction-col">

<view class="nav-block wrap">
    <block wx:for="{{navItems}}"> 
      <view class="wrap-item {{item.is_s ? 'exp' : ''}} {{item.top ? 'top' : ''}}">  
        <navigator url="../content/content?mark={{item.id}}" hover-class="navigator-hover">{{item.name}}</navigator>
      </view>
    </block>
  </view>


</view>